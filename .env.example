# -------- Core Flask --------
APP_SECRET=CHANGE_ME_LONG_RANDOM
PORT=5050
FLASK_DEBUG=0

# -------- Paths / Files --------
UPLOAD_FOLDER=uploads
USERS_FILE=users.json
DATASETS_META=datasets.json

# -------- CORS / Cookies --------
ALLOWED_ORIGINS=https://your-prod-url.onrender.com,http://127.0.0.1:5500,http://localhost:5500
SESSION_COOKIE_NAME=dd_session
SESSION_COOKIE_SAMESITE=Lax        # set to None only if using cross-site cookies over https
SESSION_COOKIE_SECURE=0            # 1 when site is HTTPS

# -------- Limits --------
MAX_UPLOAD_MB=80
CORR_MAX_SIDE=60
CORR_MAX_AREA=3600
PERMANENT_SESSION_LIFETIME=86400   # seconds

# -------- Google / Gemini (optional) --------
GOOGLE_API_KEY=
GOOGLE_CSE_ID=
GEMINI_MODEL=gemini-1.5-flash

# If you ever switch to Google Cloud creds for other APIs:
GOOGLE_APPLICATION_CREDENTIALS=/app/gcloud_credentials/credentials.json
PROJECT_ID=
LOCATION=us-central1
ASYNC_AUTO=0

# -------- Optional Celery/Redis (remove if unused) --------
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

APP_SECRET=Hamza$Victoria12